import 'package:flutter/material.dart';

class PetugasLaporanScreen extends StatelessWidget {
  const PetugasLaporanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Halaman Laporan Petugas")),
    );
  }
}